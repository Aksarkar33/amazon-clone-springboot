# APP NAME
spring.application.name=first-spring

# DATABASE CONFIGURATION (Flexible)
# If 'MYSQL_URL' exists (Cloud), use it. Otherwise, use localhost.
spring.datasource.url=${MYSQL_URL:jdbc:mysql://localhost:3306/first_spring_db}
spring.datasource.username=${MYSQL_USER:root}

# YOUR PASSWORD IS NOW SET HERE (after the colon):
spring.datasource.password=${MYSQL_PASSWORD:v0!dX@2025#Gl1tch@546sql}

# JPA / HIBERNATE
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# SECURITY (Cloud can override these, or default to admin/admin123)
spring.security.user.name=${ADMIN_USER:admin}
spring.security.user.password=${ADMIN_PASS:admin123}